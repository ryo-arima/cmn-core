Application:
  Client:
    ServerEndpoint: "http://localhost:8000"
    credentials:
      email: "admin@cmn.local"
      password: "Admin123!"

PostgreSQL:
  host: "localhost"
  user: "user"
  pass: "password"
  port: "5432"
  db: "cmn_core"
  sslmode: "disable"
